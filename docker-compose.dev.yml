version: "2"

services:
  app:
    build: docker/app/
    hostname: app
    volumes:
      - .:/var/www/html
    ports:
      - "80:80"
    environment:
      XDEBUG_HOST: ${XDEBUG_HOST}

#  db:
#      image: mysql:5.6
#      environment:
#          MYSQL_ROOT_PASSWORD: root
#          MYSQL_DATABASE: api
#      ports:
#        - "3306:3306"
